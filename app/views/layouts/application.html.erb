<!DOCTYPE html>
<html>
    <head>
      <title><%= title %></title>
      <%= csrf_meta_tags %>
      <%= render 'layouts/stylesheets' %>
    <body>
        <div class="container">
          <%= render 'layouts/header' %>
          <%= yield :nav%>
          <section class="round">
            <%= yield %>
          </section>
        </div>
    </body>
    <%= render 'layouts/footer' %>
    <br/>
    <b>Curent user: </b> <strong class="user"><%= current_user.inspect %></strong>
    <%= debug(params) if Rails.env.development? %>
</html>
